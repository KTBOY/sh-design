# 组件发布流程

> 适用于本仓库（sh-design monorepo）。从编写组件 → 写文档 → 记日志 → 升版本 → 验证 → 提交 → 发布 npm 的完整流程。
> 日常提交可直接双击根目录的 `release.bat`，它自动执行第 5～7 步。

## 流程总览

```
编写组件 → 编写文档 → 更新日志 → 升版本号 → 本地验证 → 提交推送 → 推 tag → npm + Release 全自动
   │           │           │           │           │           │              │
 src/     docs/       docs/guide/  package.json  4 条命令   CI 自动部署   OIDC 免验证码
components components/ changelog.md  (仅此一处)              文档站       publish-npm.yml
```

---

## 一、编写组件

### 1. 目录结构

新组件放在 `packages/sh-design/src/components/<组件名>/`，三件套：

```
components/
└── lazy-image/                 # 目录用 kebab-case
    ├── index.ts                # 出口：注册 install、导出类型
    └── src/
        ├── lazy-image.ts       # props / emits / 类型定义
        └── lazy-image.vue      # 组件实现（script setup）
```

### 2. 命名规范

| 项 | 规范 | 示例 |
| --- | --- | --- |
| 组件名 | `Sh` 前缀 + PascalCase，`defineOptions({ name })` 必写 | `ShLazyImage` |
| Props 定义 | `xxxProps` + `XxxProps` 类型 | `lazyImageProps` / `LazyImageProps` |
| Emits 定义 | `xxxEmits`，事件载荷用对象（便于扩展） | `{ url }` / `{ error }` |
| CSS 类名 | `sh-` 前缀 + BEM | `.sh-lazy-image__skeleton` |
| CSS 变量 | 用主题变量，不写死颜色 | `var(--sh-color-bg-hover)` |

### 3. 注册导出（漏一处就用不了）

- `packages/sh-design/src/components/index.ts` —— 加入组件导出
- `packages/sh-design/src/index.ts` —— 确认全量出口包含新组件

### 4. 编写注意事项

- **SSR 安全**：文档站是 VitePress（构建时跑 SSR）。`window` / `document` / `IntersectionObserver` 等浏览器 API 只能放在 `onMounted` 里，或加 `typeof window === 'undefined'` 守卫，否则 `docs:build` 直接崩。
- **资源清理**：定时器、observer、`URL.createObjectURL` 必须在 `onBeforeUnmount` 里回收。
- **破坏性变更**：改已有组件的 props / 事件载荷格式属于 Breaking Change，changelog 里要标 ⚠️，版本号至少升 minor（0.x 阶段可酌情 patch，但必须写清）。

---

## 二、编写文档

### 1. 组件文档页

新建 `docs/components/<组件名>.md`，包含：

- 组件一句话定位 + 适用场景
- 各功能演示（可在 md 里写 `<script setup>` 做响应式 demo）
- API 三张表：**Props / Events / Slots**（含类型和默认值）

### 2. 注册侧边栏

在 `docs/.vitepress/config.ts` 的 sidebar 中加入新页面入口。

### 3. ⚠️ 最容易踩的坑：漏提交被引用的文件

文档里 `import` 的自定义组件（如 `docs/.vitepress/theme/components/Xxx.vue`）、引用的图片 / gif（放 `docs/public/`），**必须和文档一起提交**。

> 真实案例：`lazy-image.md` import 了 `MiniProgramDialog.vue`，但该文件没提交。本地 `docs:build` 通过（文件在磁盘上），CI 干净检出后解析不到 import，`Build docs` 3 秒即失败。
> 结论：**本地绿、CI 红 → 第一个怀疑对象就是漏提交文件**。`release.bat` 用 `git add -A` 就是为了防这个。

### 4. 静态资源规范

- 站点资源放 `docs/public/`，md 里用根路径引用（如 `/mini-program-demo.gif`）
- 图片/视频等二进制格式已在 `.gitattributes` 声明 `binary`，新格式记得补一行

---

## 三、更新日志（必做）

组件有**新增 / 修改 / 修复**时，必须同步更新 `docs/guide/changelog.md`，格式：

```md
## 0.0.4

<sub>2026-07-30</sub>

### ✨ 新增
- `ShXxx` 新增 xxx 能力

### 🔧 修改
- ⚠️ `ShXxx` 的 `load` 事件载荷改为对象 `{ url }`（破坏性变更）

### 🐛 修复
- 修复 xxx 场景下 xxx 的问题
```

只记录**组件相关**改动；纯文档 / CI / 站点样式改动不记。

---

## 四、升版本号

**只改一处**：`packages/sh-design/package.json` 的 `version`。

- `src/version.ts` 由 `scripts/sync-version.mjs` 在构建时**自动同步**，不要手改
- 顺手同步 `docs/.vitepress/config.ts` 导航里的版本徽标（nav 上的 `0.0.x`）

SemVer 约定：修 bug 升 patch（0.0.3 → 0.0.4）；加功能升 minor；破坏性变更升 major（0.x 阶段从宽，但 changelog 必须标 ⚠️）。

---

## 五、本地验证（4 条命令，全过再提交）

```bash
pnpm typecheck     # vue-tsc 类型检查
pnpm lint          # eslint
pnpm build         # 构建组件库（会先自动同步 version.ts）
pnpm docs:build    # 构建文档站（和 CI 完全同款，最接近线上）
```

环境要求：**Node ≥ 22.13**（pnpm 11.7+ 硬性要求，低版本会报 `node:sqlite` 错误）。

---

## 六、提交推送

```bash
git add -A                      # -A 防止漏提交新文件（见第二节的坑）
git commit -m "feat(xxx): ..."
git push origin main
```

- 提交信息用约定式：`feat(组件名):` / `fix(组件名):` / `docs:` / `chore:`
- push 到 main 会自动触发 CI（`deploy-docs.yml`）构建并部署文档站到 GitHub Pages
- 推完去 https://github.com/KTBOY/sh-design/actions 确认绿了再走下一步
- 注意：GitHub Actions 匿名 API 看不到错误日志，CI 红了要进 Actions 页面点开看

---

## 七、发布 npm

### 正式路径：推 tag 全自动发布（推荐，零手动）

```bash
git tag -a v0.0.5 -m "v0.0.5: 一句话说明"
git push origin v0.0.5
```

推送 `v*` tag 会自动触发 `publish-npm.yml` 工作流，它会依次：

1. 构建组件库
2. 通过 **OIDC Trusted Publishing** 自动 `npm publish`（无需 token / OTP / 恢复码，自动附带 provenance）
3. 发布成功后**自动创建 GitHub Release**（自动生成 release notes）

全程零手动点击。用 `release.bat` 发版时选 Y 即可走到这一步。

> 前提：npmjs.com 上已为 `sh-design` 配置 Trusted Publisher（指向 `KTBOY/sh-design` + `publish-npm.yml`，已验证可用）。

### 备用路径：本地发布（Trusted Publisher 未配置时）

```bash
cd packages/sh-design
npm publish --access public --otp=<恢复码>
```

- **必须用 npm，不能用 pnpm**（pnpm 在安全密钥 2FA 下无法非交互式传 OTP）
- 恢复码是一次性的，用一个少一个
- `prepublishOnly` 钩子会自动重新构建，无需手动 build

### 发布后校验

- `npm view sh-design version` 应输出新版本号
- 文档站导航徽标、changelog 与 npm 版本三者一致

---

## 八、release.bat 脚本说明

### 这个脚本是干什么的

`release.bat` 是仓库根目录的**一键提交/发布助手**：把上面第 **五（本地验证）→ 六（提交推送）→ 七（打 tag 发版）** 三个手动环节串成一条流水线，双击即可执行。目的有两个：

1. **不漏步骤**——每次提交前强制跑完整验证，杜绝"推上去 CI 才发现红了"；
2. **不漏文件**——用 `git add -A` 统一提交，防止"文档引用的新文件忘了提交"这类本地绿、CI 红的坑（见第二节真实案例）。

它是**手动提交时的替代品**：以后写完组件不用逐条敲命令，直接跑它。

### 怎么运行

- 方式一：资源管理器里**双击** `release.bat`
- 方式二：终端执行 `.\release.bat`（在仓库根目录）

### 每一步做了什么（与手动流程的对应关系）

| 脚本阶段 | 做什么 | 对应手动流程 | 失败/拒绝时 |
| --- | --- | --- | --- |
| ① 环境检查 | 校验 Node ≥ 22.13（pnpm 11.7+ 硬性要求） | 第五节环境要求 | 直接中断，提示切 Node |
| ② 读版本号 | 从 `packages/sh-design/package.json` 读出当前 version | 第四节 | — |
| ③ changelog 检查 | 在 `docs/guide/changelog.md` 里找 `## 当前版本` 条目 | 第三节 | 找不到会**提醒并询问**是否继续 |
| ④ 四连验证 | `pnpm typecheck` → `lint` → `build` → `docs:build` | 第五节 | 任一失败立即中断 |
| ⑤ 提交推送 | 展示 `git status` 清单 → 你确认 → 输入提交信息 → `git add -A` + commit + push | 第六节 | 选 N 则到此结束（只验证不提交） |
| ⑥ 发版（可选） | 询问是否发版 → 检查 tag 是否已存在 → 打 `v<版本>` tag 并推送 → CI 自动发 npm + 自动建 Release | 第七节 | 选 N 则只提交不发版 |

### 运行中会问你的 3 个问题

1. **「组件有改动时必须补更新日志，仍要继续吗」**——changelog 里没有当前版本条目时才会问。正常应该选 N，先去补 changelog。
2. **「确认全部加入并提交推送吗」**——确认前**看一眼上方清单**，有不想入库的临时文件（截图、录屏等）先删掉或加 `.gitignore` 再重跑。
3. **「是否发版」**——日常提交选 N；要发 npm 新版本选 Y（前提：已升版本号 + 已写 changelog）。

### 脚本不负责的事（仍需手动）

- 写组件、写文档、写 changelog、改 `package.json` 版本号——这些是**运行脚本前**的准备工作（第一～四节）。
- 发版环节已全自动：推 tag 后 CI 自动发 npm 并创建 GitHub Release，无需任何点击。

### 注意事项

- `git add -A` 会把**所有**未跟踪文件都加进来，确认清单时务必留意；
- 日常小提交也推荐走脚本（发版那步选 N 即可），保证每次入库的代码都过了完整验证；
- 版本号没升就选择发版，脚本会因 tag 已存在而中断——这是保护，不是 bug。

---

## 常见坑速查

| 现象 | 原因 | 解法 |
| --- | --- | --- |
| 本地 `docs:build` 绿，CI 红 | 文档引用的文件没提交 | `git add -A`，或对照 `git status` 补提交 |
| `node:sqlite` 报错 | Node < 22.13 跑 pnpm 11.7+ | 升 Node |
| pnpm publish 卡在 OTP | pnpm 不支持安全密钥 2FA 非交互发布 | 用 `npm publish --otp=...` |
| 打 tag 报已存在 | 版本号没升就重复发版 | 升 `package.json` 版本后重来 |
| 组件在文档站不渲染 | 忘记在 `components/index.ts` 注册导出 | 补注册 |
| 浏览器 API 构建报错 | SSR 阶段执行了 `window` 等 | 移入 `onMounted` 或加守卫 |
